<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.mysocialfeed.supportingfiles"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="fr.mysocialfeed.supportingfiles.MainActivity"
            android:label="@string/app_acronym"
            android:icon="@drawable/ic_launcher"
            android:uiOptions="splitActionBarWhenNarrow" >
        </activity>        
        <activity
            android:name="fr.mysocialfeed.supportingfiles.LoginDisplayActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="fr.mysocialfeed.supportingfiles.ErrorActivity"
            android:label="@string/title_activity_error" >
        </activity>
    </application>

</manifest>